import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

<Meta title="Getting Started/Build Tools" />

# Build Tools

A production static version of this storybook can be built into the `public` directory

A script can be ran that will automatically deploy to a static bucket like AWS s3, netlify
